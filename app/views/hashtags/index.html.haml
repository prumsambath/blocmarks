%h2
  Hashtags
%hr
%div
  A hashtag is label that groups your bookmarks.
%p
%div.row
  - @hashtags.each do |hashtag|
    .col-md-2
      %span
        = link_to hashtag.text, hashtag_path(hashtag.text), class: 'btn btn-info btn-small'
      %p
        = pluralize(hashtag.bookmarks.count, 'bookmark')
